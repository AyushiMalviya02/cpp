#include <iostream>
using namespace std;

int main() {
    long long a, b;
    cout << "Enter two numbers: ";
    cin >> a >> b;

    while (b != 0) {
        long long temp = b;
        b = a % b;
        a = temp;
    }

    cout << "GCD is: " << a;

    return 0;
}
