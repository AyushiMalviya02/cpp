#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double a, b;
    cout << "Enter base and exponent: ";
    cin >> a >> b;

    double result = pow(a, b);

    cout << a << "^" << b << " = " << result;

    return 0;
}
